# Report: run 8b, app install and lifecycle (Python)

Date 2026-09-08. Branch `app` (worktree `.claude/worktrees/app`). Full account in [task-report.md](./task-report.md); the review in [../review-0.13.0.md](../review-0.13.0.md).

- T-71: done — `dictate.session` carries `state` (`starting` at claim, `recording` right after `_launch_recorder` in both `_start` and `listen()`, `transcribing` in `_mark_finishing`) and a per-claim `nonce` (`secrets.token_hex(8)`), rewritten in place with the existing `O_NOFOLLOW` 0600 pattern and only when the file's `dir` is this workdir; `_read_session` already ignored unknown keys (now pinned); `clip` exits 3 on the credential refusal.
- T-72: done — `vocalize/app.py` and the `app install/uninstall/status/restart` group: `APP_SPEC` (AppKit and Carbon, no entitlements, `Vocalize.app`, `vocalize-app`, stamp `.app`) built by `build_bundle` under `~/Library/Application Support/vocalize`; the LaunchAgent via `plistlib` (0644, `O_NOFOLLOW`); bootout then bootstrap; `tccutil reset Accessibility` only on `rebuilt` with `APP_REGRANT_WARNING`; liveness from `launchctl list <label>` (a PID line is loaded, "could not find service" is not running, anything else unknown); the binary-discovery check against `sys.argv[0]` with the quoted `defaults write` line; the Hammerspoon and Services warnings; `app.status` read with `O_NOFOLLOW`, `O_NONBLOCK`, a regular-file check, a 64 KiB cap and a closed vocabulary; `kickstart -k`; uninstall removes exactly the plist, bundle, stamp, `app.status`, `app.log`, `dictate.copied` and the `defaults` override, refuses symlinks, leaves the recorder and the Accessibility entry.
- T-73: done — readiness rows `app`, `app agent`, `accessibility`, registered when the bundle exists; `/api/state` gains `app` from `app.status_dict()`.
- T-74: done — `FakeToolchain` shared from `conftest.py`; `tests/test_app_build.py` covers the app's layout, stamp, rebuild rule and argv, with the real `swiftc -parse` and `plutil -lint` checks skipped without the tools.
- T-75: done — `docs/app.md`; README hotkey section (the app first, the Quick Action as the fallback) and the "safe to delete" sentence narrowed to exclude `bin/`; `docs/dictation.md` hotkey sections; CHANGELOG.
- T-76: done — `review-0.13.0.md` (app slice): three lenses, three refuters per finding; 14 raw, 7 confirmed (3 medium, 4 low), 7 refuted with reasons; the mediums (a 30 s liveness read on the portal's request thread; a FIFO at `app.status` hanging every reader) fixed by the review's fixer; the three lows (a symlinked plist path crashing install, the override line unquoted, a bundle without its plist reading as current) fixed by the release manager after the review. No critical or high finding exists or is open.

Security gate (tests, all green): `test_every_tool_call_is_a_list_argv_with_no_shell_and_a_timeout`, `test_tccutil_runs_only_when_the_bundle_was_rebuilt`, `test_install_boots_out_before_it_bootstraps`, `test_a_bootstrap_failure_names_the_command`, `test_install_writes_the_plist_over_a_tighter_umask`, `test_app_status_is_parsed_as_untrusted_words` (9 cases), `test_an_oversized_app_status_reads_as_unknown`, `test_a_symlinked_app_status_is_not_followed`, `test_a_fifo_at_app_status_reads_as_unknown_and_does_not_block`, `test_launchctl_list_is_read_conservatively` (7 cases), `test_uninstall_removes_its_own_files_and_nothing_beside_them`, `test_uninstall_refuses_to_follow_a_symlinked_bundle`, `test_the_liveness_read_is_bounded_by_the_poll_budget_not_the_install_one`, `test_a_symlinked_plist_path_is_refused_with_a_message_not_a_traceback`, `test_the_override_line_is_quoted_for_a_path_with_a_space`, `test_a_bundle_missing_its_plist_reads_as_stale` — tests/test_app_cli.py; `test_no_session_state_survives_any_exit_path` (4 cases), `test_every_claim_mints_a_fresh_nonce`, `test_a_session_a_later_press_owns_keeps_its_state` — tests/test_dictate.py; the readiness and portal `-k app` tests.

Real build on this Mac (owner present, 2026-09-08 15:34): with `VocalizeBinary` pointed at this worktree's CLI (the owner's everyday tool is still 0.12.0), `vocalize app install --yes` compiled and signed the app first time, wrote the LaunchAgent and bootstrapped it; `app status --json` reported `bundle: current`, `agent: loaded`, `accessibility: not granted` (no speak-selection yet), `hotkeys: ok`, `hotkey_backend: carbon`, `vocalize: <the worktree CLI>`; `vocalize status` showed the three new rows. The owner's look-at-it results (icon, menu, a dictation, the first speak-selection and its Accessibility prompt) are recorded in task-report.md § Owner check.

Deferred: nothing. Manual checks 5–8 belong to run 10.

Suite: 2055 passed, 3 skipped. Ruff clean. Gate 12 of 12.

validate-exit: PASS
