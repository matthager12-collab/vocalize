# Report: run 10, release 0.13.0

Date 2026-09-08. Branch `app` (worktree `.claude/worktrees/app`). Full account in [task-report.md](./task-report.md); the review in [../review-0.13.0.md](../review-0.13.0.md) § Release review.

- T-90: done — `__version__` 0.13.0; CHANGELOG `## 0.13.0 - 2026-09-08` with an empty Unreleased above it; docs-match-CLI passes for the ten 0.13.0 commands; README gained `### vocalize doctor` and the `[app]` table; docs/roadmap.md's row reads in progress with both releases named.
- T-91: done — the release review (five lenses over runs 7–9, three refuters per finding): 25 raw, 24 triaged, 16 refuted, 12 confirmed (6 medium, 6 low), 4 refuted with reasons, 8 past the cap checked once by the release manager. Mediums fixed: session readers never raise on non-object JSON (a torn session could have kept the microphone open); `integrate claude` refuses a non-executable or relative vocalize path; the doctor survives a bad config as one failed row; the `[app]` table is documented; the services-conflict doctor row that warned about the bundle `integrate claude` installs is gone and `app install` names the real conflict (an assigned D or X shortcut). One medium accepted: speak-selection presses queue on the serial queue in the frozen Swift (a re-grant to change; the stop chord is the escape). Lows fixed after the review by the release manager: stale `app.status` unlinked on a rebuild, the skill's refuse list, `pbs -update` timeout, four doc corrections. Three lows open and named (which vocalize the app found is not reported; `discoverable()` is a membership test; no crash-leftover sweep in `app uninstall`). No critical or high finding exists or is open in any of the file's three sections.
- T-92: done — manual checks with the owner present: 4 (run 7's spike, Carbon on three frontmost apps); 5 (icon, menu, a dictation with the red icon — the stopwatch latency numbers were skipped by the owner's choice); 6 (speak-selection with the Accessibility prompt once; the X chord silences a read within a second); 7 (`kill -9` relaunched in under 12 s; Quit stayed quit; `app restart` brought it back; log out and in deferred to the owner's next login); 8 (the released tool installed with the app running — see task-report.md). Published from this worktree's `dist/` with the token injected by `op run`; PyPI digests equal the local build.

Security gate: the gate's own rows plus the review's tests — `test_session_json_that_is_not_an_object_never_raises` (4 cases), `test_refuses_a_vocalize_path_that_is_not_executable`, `test_refuses_a_relative_vocalize_path`, `test_doctor_reports_a_bad_config_as_one_failed_row`, `test_no_doctor_row_warns_about_a_quick_action_integrate_installs`, `test_install_warns_about_a_shortcut_not_about_the_quick_action_itself`, `test_a_rebuild_forgets_the_old_status_file`, `test_bootstrap_retries_once_after_launchd_says_io_error`, `test_bootstrap_does_not_retry_a_success`.

Incident: a release-review finder wrote probe scripts under /tmp that drove `app install` and `app uninstall` through `CliRunner` with only `APP_DIR` redirected; `defaults` and `launchctl` ignore HOME, so the owner's real LaunchAgent plist was deleted, the real `VocalizeBinary` override was removed and the real agent was booted out. The main session found the app running on the 0.12.0 CLI with no chords, restored the override, re-ran `app install` (which exposed launchd's bootstrap-after-bootout error 5, now retried once in `app.bootstrap()`), and verified the grant intact. The standing agent rule now names this trap.

Deferred: the log-out-and-in half of manual check 7 (the owner's next login; RunAtLoad is in the plist).

Suite: 2122 passed, 3 skipped. Ruff clean. Gate 16 of 16 after publish.

validate-exit: PASS
