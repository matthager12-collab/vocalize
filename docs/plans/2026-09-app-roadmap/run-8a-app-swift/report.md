# Report: run 8a, the menu-bar app (Swift)

Date 2026-09-08. Branch `app` (worktree `.claude/worktrees/app`). Full account in [task-report.md](./task-report.md).

- T-70: done — `vocalize/menubar/VocalizeApp.swift` (1560 lines) and `vocalize/menubar/Info.plist.in`, complete: status item with the four session states (idle, starting, recording in red, transcribing) and the named `unrecognisedState` default branch (DEC-036); both hotkey backends compiled, one build-time constant set to Carbon (DEC-033); the dispatch table (toggle drops a press while a dictate child runs; hold maps key-down and key-up to `dictate --start`/`--stop`; stop on the concurrent queue); speak-the-selection with the Accessibility prompt, `changeCount` polling and `vocalize clip` (exit 3 mapped to a fixed notice); the spawn contract in one `run()` (fresh environment, cwd tmp, stdin/stdout null, stderr to the 0600 capped `app.log`, one re-resolve on ENOENT/126/127, a wall-clock cap with SIGTERM then SIGKILL so no queue can wedge); `checkedBinary` with the `VocalizeBinary` override checked like every candidate; the config-directory watcher and coalesced `settings` re-read with last-good settings kept and a three-step retry; the cache watcher plus a half-second poll of `dictate.session` (a directory vnode source never sees an in-place rewrite); `app.status` on launch and every dispatch, atomic and behind a lock; `pasteIfNonceMatches` (marker unlinked through an open descriptor first, then paste on, nonce, two-sided freshness, same frontmost application); fixed-string notices flashed in the status item's title; the seven menu items; `keyCodes` with exactly the 48 names of `config.CHORD_KEYS`.

Security gate: `grep -c "NSPasteboard\.string\|readObjects"` = 0; `grep -c "func checkedBinary\|func pasteIfNonceMatches"` = 2; `osascript` absent; `xcrun swiftc -typecheck` and `-parse` clean, `plutil -lint` OK; `tests/test_config.py::test_the_chord_key_allowlist_matches_the_swift_keycode_table` passes (no longer skipped). Process: two independent Opus drafts, three judges (draft A won all three lenses; ten grafts from B), a merge, three critic lenses, then a targeted fix round with two recheck lenses reporting CLEAN; the release manager read the whole file and fixed one more defect (the `portal` child was capped at 60 s).

Deviations from the design, all recorded in the source: notices are a four-second flash of the status item's title rather than `UNUserNotificationCenter`, because that API aborts the process when LaunchServices has no record of the bundle and this bundle is launched by a LaunchAgent from `~/Library/Application Support`; `vocalize settings` is the one child whose stdout is piped; `settings` and `portal` run on the concurrent queue; a session that is present but does not parse yet reads as `starting` for one second; paste-off returns silently instead of showing "Copied, not pasted".

Gate defects fixed: `xcrun swiftc -parse` over both Swift files can never pass (each is a single-file program with top-level code), so the gate and verification.md parse one file per invocation; "source committed" and "work committed" now see untracked files.

Contract for run 8b (Python): write `state` (starting/recording/transcribing; rewritten in place is fine, the app polls) and `nonce` into `dictate.session`; write `dictate.copied` (0600, `O_NOFOLLOW`) as JSON `{"epoch": <seconds>, "nonce": "<hex>"}` after the clipboard copy (0.13.1); add `dictate --start`/`--stop` (0.13.1); print `stt.paste` from `settings` (0.13.1); `APP_SPEC` with frameworks AppKit and Carbon, no entitlements, bundle `Vocalize.app`, binary `vocalize-app`, stamp `.app`; no LaunchServices registration is required by this source.

Deferred: nothing. The SF Symbol names (mic, hourglass, mic.fill, waveform, exclamationmark.triangle) and the title-flash notices are a look-at-it check for the owner in run 8b or the Phase 10 manual checks, before the source freezes at 0.13.0.

Suite: 1971 passed, 3 skipped (the keycode test now runs). Ruff clean. Gate 16 of 16.

validate-exit: PASS
